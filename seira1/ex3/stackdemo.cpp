#include <iostream>
#include <vector>

using namespace std;

template <typename T>
class stack{
    public:
    stack(int size)
    stack(const stack &s)
    ~stack()
    const stack & operator = (const stack &s) {
    

}